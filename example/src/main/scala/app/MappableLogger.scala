package app

//import mappable.{Mapify, Mappable}

//object MappableLogger {
//  def log[E : Mappable](message: Message[E]): Map[String, Any] = {
//    Mapify.toMap(message.data) + ("name" -> message.name)
//  }
//}
